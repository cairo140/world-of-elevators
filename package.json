{
  "private": true,
  "engines": {
    "node": ">=4.1 <5",
    "npm": ">=3.1 <4"
  },
  "dependencies": {},
  "devDependencies": {
    "jest-cli": "^0.8.0",
    "tslint": "^3.2.2",
    "typescript": "^1.7.5"
  },
  "jest": {
    "rootDir": "./lib"
  },
  "scripts": {
    "lint": "tslint src/**/*.ts",
    "postinstall": "tsd reinstall",
    "test": "tsc && jest"
  }
}
