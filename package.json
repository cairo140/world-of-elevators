{
  "private": true,
  "engines": {
    "node": ">=4.1 <5",
    "npm": ">=3.1 <4"
  },
  "dependencies": {},
  "devDependencies": {
    "jest-cli": "^0.8.0",
    "tslint": "^3.2.2",
    "typescript": "^1.7.5",
    "typings": "^0.6.9"
  },
  "jest": {
    "rootDir": "./lib"
  },
  "scripts": {
    "lint": "tslint src/**/*.ts",
    "postinstall": "typings install",
    "test": "tsc && jest"
  }
}
